name: Deploy Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: cloudflare-pages-production
  cancel-in-progress: true

permissions:
  contents: read
  deployments: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          # Full history: the sitemap's lastmod is the design file's last commit
          # date, and a shallow clone cannot resolve it.
          fetch-depth: 0

      # Rebuild rather than trusting the committed public/ directory: if the
      # design was edited without re-running the build, this is where it fails,
      # instead of publishing a page that no longer matches its source.
      - name: Build site
        run: python3 scripts/build.py

      # sitemap.xml is excluded on purpose. Its lastmod is the design file's
      # last commit date, and the sitemap is committed *in* that same commit,
      # so the committed copy can never match what this rebuild produces --
      # every design change would fail here on its first push. Nothing is lost:
      # the deploy publishes what this step just rebuilt, not what was
      # committed, so the shipped sitemap is always freshly generated.
      - name: Fail if the build output was stale
        run: |
          if ! git diff --quiet -- public/ ':!public/sitemap.xml'; then
            echo "::error::public/ is out of date with design/ — run 'python3 scripts/build.py' and commit the result."
            git --no-pager diff --stat -- public/ ':!public/sitemap.xml'
            exit 1
          fi

      - name: Deploy production site
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy public --project-name=learn-terraform --branch=main --commit-hash=${{ github.sha }}
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
